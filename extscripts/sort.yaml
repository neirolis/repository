name: SORT
desc: tracking by SORT...
version: v1.0.0

source: https://github.com/MASLINQUE/sort_extscript/releases/download/1.0.0/sort.zip

exec: 
  - mkdir -p $WD/extscripts
  - cp -r $PKGDIR/* $WD/extscripts/
  - python3 -m pip install -r /extscripts/sort/requrequirements.txt
