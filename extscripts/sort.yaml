name: SORT
desc: tracking by SORT...
version: v1.0.0

source: https://github.com/MASLINQUE/sort_extscript/releases/download/1.0.0/sort_tr.zip

exec: 
  - mkdir -p $WD/extscripts
  - cp -r $PKGDIR/* $WD/extscripts/
  
need_restart: false

