type: plugins
name: cameras-multistreams
desc: Checking `http` video streams availability and switching cameras address on failures
version: v0.4.1

source: https://rtmip.info/plugins/cameras-multistreams.zip

exec:
  - cp -f $PKGDIR/cameras-multistreams.so $WD/plugins/
  - cp -rf $PKGDIR/multistreams $WD/pages/multistreams

need_restart: true
