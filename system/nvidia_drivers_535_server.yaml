name: nvidia_drivers_535_server
desc: Install Nvidia 535 Server driver. Attention! ONLY MANUAL UNINSTALLATION
version: 0.99
depends:
  - nvidia_apt_repo
exec:
  - apt install -y nvidia-driver-535-server

need_restart: false
